#include <stdlib.h>
#include <stdio.h>

	int strlen(char s[])
{

	int count = o;

	while (s[count] != 0)

	{
	 count++;
	 return count;
	}
}

void main()
{
	char  x =" xyyzy";
	char  y = "permission";
	 printf("%d %d \n", strlen(x), strlen(y));
